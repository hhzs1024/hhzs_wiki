# 正则表达式

匹配数字：比如V=3600，使用`V=\d{4}`

排除前缀：比如pre V=3600，off V=3600，想要排除pre，使用`(?<!pre )V=\d{4}`

匹配前缀：比如smokepre, V=3000, 想要提取这个前缀的数字，使用`(?<=smokepre, V=)\d{4}`

匹配多个数字：\d*或者\d+，\*是0-无限个，+是1-无限个

vscode中批量选取匹配到的字符`alt+enter`，用于选取串口打印数据 